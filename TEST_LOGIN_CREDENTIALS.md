# Test Login Credentials

Use these demo credentials to sign in:

- Email: `admin@example.com`
- Password: `password123`

Successful login returns a token from the backend and the frontend stores it in browser local storage for protected routes.